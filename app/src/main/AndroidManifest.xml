<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.kth.csc.iprog.dinnerplanner.android" >

    <application
        android:name=".DinnerPlannerApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        >
    <!--    <activity
            android:name=".Activity2_MenuView"
            android:label="@string/app_name" >
        </activity>-->
        <activity
            android:name=".Activity2_MenuView"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:parentActivityName=".Activity2_MenuView"
            android:theme="@style/AppTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="se.kth.csc.iprog.dinnerplanner.android.MainActivity" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PageFinal"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="se.kth.csc.iprog.dinnerplanner.android.MainActivity" />
        </activity>

    </application>

</manifest>
